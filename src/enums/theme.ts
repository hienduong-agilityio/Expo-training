export enum BUTTON_VARIANTS {
  SOLID = 'solid',
  OUTLINE = 'outline',
  GHOST = 'ghost',
}

export enum BUTTON_COLORS {
  PRIMARY = 'primary',
  NEUTRAL = 'neutral',
}

export enum TEXTFIELD_VARIANTS {
  OUTLINED = 'outlined',
  FILLED = 'filled',
}

export enum TEXTFIELD_STATES {
  DEFAULT = 'default',
  FOCUSED = 'focused',
  ERROR = 'error',
  DISABLED = 'disabled',
}
